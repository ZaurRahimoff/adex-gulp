# 🎨 Control Panel - Интерактивная панель управления проектом

## 📋 Описание

Control Panel - это веб-интерфейс для управления Gulp-проектом прямо из браузера. Вместо работы в командной строке, вы можете настраивать проект через удобный графический интерфейс.

## 🚀 Запуск

После запуска `npm run dev` автоматически откроется **Control Panel** на `http://localhost:3000/control-panel.html`

## ✨ Возможности

### 📄 Навигация по страницам
- **Левая панель**: Список всех HTML страниц проекта
- Клик по странице загружает её в preview-окне
- Автоматическое сканирование страниц в `dist/`

### 📱 Переключение viewport
- **Верхняя панель**: Кнопки для переключения размеров экрана
  - 📱 **Mobile** (375px)
  - 📱 **Tablet** (768px)  
  - 💻 **Desktop** (1440px)
- Изменения сохраняются в конфигурации

### 📚 Управление библиотеками
- **Правая панель → Библиотеки**:
  - ✅ Установка/удаление библиотек одним кликом
  - Доступные библиотеки:
    - Bootstrap
    - Font Awesome
    - Swiper
    - jQuery
    - GSAP
- Автоматическое обновление `main.scss` и конфигурации

### 🎨 Редактор цветов
- **Правая панель → Цвета**:
  - Color picker для всех основных цветов
  - Primary, Secondary, Gray, Background
  - Live preview (после сохранения нужно перезапустить сборку)
  - Автоматическое обновление `src/scss/variables/_colors.scss`

### 🔤 Типографика (Google Fonts)
- **Правая панель → Типографика**:
  - Выбор из популярных Google Fonts
  - Автоматическое добавление в конфигурацию
  - Обновление `src/scss/variables/_typography.scss`

### 🧩 Конструктор компонентов
- **Левая панель → Конструктор**:
  - Создание компонентов прямо из интерфейса
  - Выбор готовых сниппетов:
    - Кнопка
    - Карточка
    - Header
    - Footer
    - Navbar
    - Базовый шаблон
  - Автоматическая генерация PUG, SCSS, JS файлов

## 🌐 API Endpoints

Все API доступны на `http://localhost:3002/api`:

- `GET /api/pages` - Список HTML страниц
- `GET /api/config` - Текущая конфигурация
- `POST /api/config` - Обновление конфигурации
- `GET /api/fonts` - Список Google Fonts
- `POST /api/library/:name` - Установка/удаление библиотеки
- `POST /api/component` - Создание компонента
- `POST /api/viewport` - Изменение viewport размера

## 📖 Использование

### Установка библиотеки
1. Откройте Control Panel
2. В правой панели найдите секцию "📚 Библиотеки"
3. Отметьте нужную библиотеку
4. Дождитесь завершения установки

### Изменение цветов
1. Откройте секцию "🎨 Цвета"
2. Выберите цвет с помощью color picker
3. Нажмите "💾 Сохранить цвета"
4. Перезапустите сборку (`Ctrl+C` и `npm run dev`)

### Создание компонента
1. В левой панели найдите "🧩 Конструктор"
2. Введите имя компонента (например: `productCard`)
3. Выберите сниппет (или оставьте "Базовый")
4. Нажмите "Создать компонент"
5. Файлы появятся в `src/pug/components/`, `src/scss/components/`, `src/js/modules/`

### Переключение viewport
1. Нажмите на нужную кнопку в верхней панели
2. Preview-окно автоматически изменит размер
3. Конфигурация обновится автоматически

## 🔧 Технические детали

- **API Server**: Express сервер на порту 3002
- **BrowserSync**: Интеграция через middleware
- **Live Reload**: Автоматическое обновление при изменениях
- **Конфигурация**: Все изменения сохраняются в `gulp-config.json`

## 🎯 Советы

- После изменения цветов **перезапустите сборку** для применения изменений
- После установки библиотеки проверьте, что она добавлена в `main.scss`
- Используйте viewport switcher для тестирования responsive дизайна
- Созданные компоненты автоматически подключаются через BEM генератор

## 🐛 Устранение неполадок

**Control Panel не открывается:**
- Убедитесь, что порт 3000 свободен
- Проверьте, что `dist/control-panel.html` существует

**API не отвечает:**
- Проверьте, что порт 3002 свободен
- Убедитесь, что API сервер запущен (должен стартовать вместе с BrowserSync)

**Библиотека не устанавливается:**
- Проверьте подключение к интернету
- Убедитесь, что имя библиотеки корректное
- Проверьте права доступа к `node_modules`

---

**Приятной работы с Control Panel! 🚀**

