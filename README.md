# 🚀 Продвинутая сборка Gulp 4 для фронтенд-разработки

Современная, оптимизированная сборка Gulp 4 с компонентным подходом, включающая все необходимые инструменты для эффективной фронтенд-разработки.

## 📋 Содержание

- [Возможности](#возможности)
- [Установка](#установка)
- [Быстрый старт](#быстрый-старт)
- [Структура проекта](#структура-проекта)
- [Команды](#команды)
- [Компонентная архитектура](#компонентная-архитектура)
- [BEM генератор](#bem-генератор)
- [SEO валидация](#seo-валидация)
- [Эмуляция устройств](#эмуляция-устройств)
- [Линтинг](#линтинг)
- [Расширение](#расширение)

## ✨ Возможности

### 🔧 Технологии
- **Pug** → компиляция в HTML с компонентами
- **SCSS** → компиляция в CSS с автопрефиксером и минификацией
- **JavaScript ES6** → сборка модулей в один файл с Babel
- **Изображения** → оптимизация JPG/PNG (~80%) + генерация WebP
- **SVG** → оптимизация и генерация sprite
- **Шрифты** → автоматическая конвертация и миксины

### 🎯 Основные функции
- ✅ Компонентная архитектура (Pug + SCSS + JS)
- ✅ Интерактивный CLI для настройки проекта
- ✅ BEM генератор блоков и элементов
- ✅ SEO валидация и мета-генератор
- ✅ Автоматическое обновление браузера (BrowserSync)
- ✅ Эмуляция устройств (Mobile/Tablet/Desktop)
- ✅ Линтинг с автофиксом (ESLint, Stylelint, Prettier)
- ✅ Красивые цветные логи с временем выполнения
- ✅ Source maps для отладки
- ✅ Два варианта файлов (normal + min) для CSS и JS

## 📦 Установка

1. Клонируйте или скачайте проект
2. Установите зависимости:

```bash
npm install
```

## 🚀 Быстрый старт

### Первый запуск

При первом запуске сборка предложит настроить проект:

```bash
npm run dev
```

Вас попросят ввести:
- **Цвета проекта** (primary, secondary, gray, background)
- **Шрифты** (основной и fallback)
- **Border-radius** и ширину контейнера
- **Библиотеки** для подключения (Bootstrap, Font Awesome, Swiper, jQuery, GSAP)

После настройки создастся файл `gulp-config.json` с вашими параметрами.

### Режим разработки

```bash
# Обычный режим (Desktop 1440px)
npm run dev

# Mobile (375px)
npm run dev -- --mobile

# Tablet (768px)
npm run dev -- --tablet

# Desktop (1440px)
npm run dev -- --desktop
```

### Production сборка

```bash
npm run build
```

Результат будет в папке `dist/`:
- HTML файлы
- CSS: `normal.css` и `normal.min.css`
- JS: `script.js` и `script.min.js`
- Изображения: `compressed/` и `webp/`
- SVG спрайт
- Шрифты

## 📁 Структура проекта

```
world-art/
├── src/                    # Исходные файлы
│   ├── pug/
│   │   ├── pages/         # Страницы (компилируются в HTML)
│   │   ├── layouts/        # Шаблоны страниц
│   │   ├── mixins/         # Pug миксины
│   │   └── components/     # Компоненты
│   ├── scss/
│   │   ├── base/           # Базовые стили
│   │   ├── components/     # Стили компонентов
│   │   ├── mixins/         # SCSS миксины
│   │   ├── variables/      # Переменные (автогенерируются)
│   │   ├── vendors/         # Стили библиотек
│   │   ├── pages/          # Стили страниц
│   │   └── main.scss       # Главный файл
│   ├── js/
│   │   ├── modules/        # ES6 модули
│   │   ├── vendor/         # Внешние библиотеки
│   │   └── main.js         # Точка входа
│   ├── img/                # Исходные изображения
│   ├── icons/               # SVG иконки
│   └── fonts/               # Шрифты
├── gulp/
│   ├── config/             # Конфигурация
│   ├── tasks/              # Gulp задачи
│   ├── templates/          # Шаблоны (SVG sprite)
│   └── utils/              # Утилиты
├── dist/                   # Собранный проект
└── gulpfile.js             # Главный файл Gulp
```

## 🎮 Команды

### Основные команды

```bash
# Разработка с автоперезагрузкой
npm run dev

# Production сборка
npm run build

# Очистка dist
npm run clean
```

### BEM генератор

```bash
# Интерактивный режим
npm run bem

# Создание блока
npm run bem -- --block header

# Создание элемента
npm run bem -- --elem logo --block header

# Блок с сниппетом
npm run bem -- --block button --snippet button
```

### Линтинг

```bash
# Проверка кода
npm run lint

# Автоисправление
npm run lint:fix
```

### SEO

```bash
# Валидация SEO
gulp seo

# Генерация SEO миксина
gulp seoGenerate
```

## 🧩 Компонентная архитектура

Каждый компонент состоит из трех файлов:

1. **Pug** (`src/pug/components/_component.pug`) - разметка
2. **SCSS** (`src/scss/components/_component.scss`) - стили
3. **JS** (`src/js/modules/component.js`) - логика

### Пример компонента

**Pug:**
```pug
// src/pug/components/_button.pug
button.button Кнопка
```

**SCSS:**
```scss
// src/scss/components/_button.scss
.button {
  padding: 12px 24px;
  background: $primary;
  // ...
}
```

**JS:**
```js
// src/js/modules/button.js
export default class Button {
  constructor(element) {
    this.element = element;
    this.init();
  }
  // ...
}
```

### Использование компонента

**В Pug:**
```pug
include ../components/button
```

**В SCSS:**
```scss
@import 'components/button';
```

**В JS:**
```js
import './modules/button';
```

## 🎨 BEM генератор

Генератор создает файлы для блоков и элементов согласно методологии BEM.

### Доступные сниппеты:
- **section** - секция с контейнером
- **button** - кнопка
- **card** - карточка
- **grid** - сетка
- **modal** - модальное окно
- **header** - шапка сайта
- **footer** - подвал сайта
- **base** - базовый шаблон

### Примеры:

```bash
# Создать блок header с сниппетом
npm run bem -- --block header --snippet header

# Создать элемент logo в блоке header
npm run bem -- --elem logo --block header
```

Автоматически создадутся:
- `src/pug/components/_header.pug`
- `src/scss/components/_header.scss`
- `src/js/modules/header.js`

## 🔍 SEO валидация

### Проверка включает:
- ✅ Наличие и длина `<title>` (30-60 символов)
- ✅ Наличие и длина `meta description` (120-160 символов)
- ✅ Наличие `og:image`
- ✅ Наличие `keywords`
- ✅ Наличие `canonical`
- ✅ Единственный `<h1>` на странице
- ✅ Наличие `alt` у всех изображений

### Генерация SEO миксина

```bash
gulp seoGenerate
```

Создаст миксин `src/pug/mixins/_meta-seo.pug` для использования в layout файлах.

## 📱 Эмуляция устройств

При запуске `npm run dev` можно указать устройство:

```bash
npm run dev -- --mobile   # 375x667
npm run dev -- --tablet   # 768x1024
npm run dev -- --desktop  # 1440x900
```

BrowserSync откроется с соответствующим размером. Для изменения размера используйте DevTools браузера.

## 🧹 Линтинг

### ESLint (JavaScript)
- Проверка синтаксиса ES6+
- Интеграция с Prettier
- Автоисправление стиля кода

### Stylelint (SCSS)
- Проверка BEM naming convention
- Стандарты SCSS
- Автоисправление форматирования

### Prettier
- Единый стиль кода
- Автоматическое форматирование

### Автофикс

```bash
npm run lint:fix
```

Исправляет большинство ошибок автоматически.

## 🔌 Расширение

Для добавления новой задачи:

1. Создайте файл в `gulp/tasks/your-task.js`:

```js
const { src, dest } = require('gulp');
const logger = require('../utils/logger');

function yourTask() {
  const taskName = 'Your Task';
  logger.taskStart(taskName);
  
  return src('src/path/**/*.ext')
    .pipe(dest('dist/path'))
    .on('end', () => {
      logger.success('Задача выполнена', taskName);
    });
}

module.exports = yourTask;
```

2. Импортируйте в `gulpfile.js`:

```js
const yourTask = require('./gulp/tasks/your-task');

exports.yourTask = yourTask;
```

3. Используйте:

```bash
gulp yourTask
```

## 📚 Дополнительная информация

### Настройка библиотек

Библиотеки настраиваются при первом запуске через CLI меню. После выбора:
- В `src/scss/vendors/` добавятся стили (если нужно)
- В `src/js/vendor/` добавятся скрипты (если нужно)

### Переменные проекта

Переменные автоматически генерируются в:
- `src/scss/variables/_colors.scss`
- `src/scss/variables/_typography.scss`
- `src/scss/variables/_variables.scss`

### Конфигурация

Все настройки сохраняются в `gulp-config.json`. Можно редактировать вручную или перезапустить настройку, удалив файл.

### BrowserSync

- Основной порт: `http://localhost:3000`
- UI порт: `http://localhost:3001`
- Автоматическая синхронизация между устройствами
- Hot-reload при изменении файлов

## 🐛 Устранение неполадок

### Проблемы с зависимостями
```bash
rm -rf node_modules package-lock.json
npm install
```

### Ошибки компиляции
- Проверьте синтаксис Pug/SCSS/JS
- Посмотрите логи в консоли (они цветные и информативные)
- Запустите линтеры: `npm run lint`

### BrowserSync не работает
- Проверьте, что порт 3000 свободен
- Убедитесь, что папка `dist/` существует и содержит HTML файлы

## 📄 Лицензия

MIT

## 🤝 Поддержка

Если возникли вопросы или проблемы, проверьте:
1. Все зависимости установлены
2. Node.js версии 14+
3. Правильность структуры проекта
4. Логи в консоли для деталей ошибок

---

**Успешной разработки! 🚀**

