# 🎨 Компоненты World Art Atlas

## 📋 Список созданных компонентов

Все компоненты следуют BEM методологии и включают Pug, SCSS и JS файлы.

### 1. Topbar (`_topbar.pug`, `_topbar.scss`, `topbar.js`)

Верхняя панель навигации с двумя состояниями.

**Состояния:**
- **Не авторизован**: Кнопки "Войти" и "Регистрация"
- **Авторизован**: Аватар пользователя, имя, кнопка "Выйти"

**Использование:**
```pug
- var isAuth = false // или true
include ../components/_topbar
```

**Особенности:**
- Sticky позиционирование
- Backdrop blur эффект
- Плавные переходы и анимации
- Адаптивный дизайн

---

### 2. Navbar (`_navbar.pug`, `_navbar.scss`, `navbar.js`)

Основное навигационное меню.

**Элементы:**
- Список ссылок навигации
- Активное состояние ссылок
- Адаптивное мобильное меню (бургер)

**Использование:**
```pug
include ../components/_navbar
```

**Особенности:**
- Sticky позиционирование (под topbar)
- Подчеркивание активной ссылки
- Мобильное меню с анимацией
- Доступность (ARIA атрибуты)

---

### 3. Footer (`_footer.pug`, `_footer.scss`, `footer.js`)

Подвал сайта с полной структурой.

**Секции:**
- О проекте (логотип, описание, социальные сети)
- Навигация
- Информация
- Правовая информация

**Использование:**
```pug
include ../components/_footer
```

**Особенности:**
- Градиентный фон
- Социальные сети
- Адаптивная сетка
- Анимированное сердце в "Сделано с ♥"

---

### 4. Artwork Card (`_artwork-card.pug`, `_artwork-card.scss`, `artwork-card.js`)

Карточка произведения искусства с hover эффектами.

**Элементы:**
- Изображение с overlay
- Кнопки действий (просмотр, лайк, поделиться)
- Информация (художник, год, местоположение)
- Описание и теги

**Использование:**
```pug
include ../components/_artwork-card
- var artworkId = "1"
- var title = "Название"
- var artist = "Художник"
- var year = "2024"
- var location = "Местоположение"
- var description = "Описание"
- var tags = ["Тег1", "Тег2"]
- var image = "img/compressed/image.jpg"
```

**Особенности:**
- Hover эффекты (поднятие карточки, увеличение изображения)
- Overlay с кнопками действий
- Адаптивная высота
- Интеграция с модальными окнами

---

### 5. Modal (`_modal.pug`, `_modal.scss`, `modal.js`)

Модальные окна для просмотра произведений (2 варианта).

**Вариант 1:** Полное модальное окно (`artwork-modal`)
- Большое изображение
- Детальная информация
- Кнопки действий
- Рейтинг и теги

**Вариант 2:** Малое модальное окно (`info-modal`)
- Компактная форма
- Текстовая информация

**Использование:**
```pug
// Автоматически включено в layout
// Открытие через JavaScript
Modal.open('artwork-modal');
```

**Особенности:**
- Backdrop blur
- Закрытие по Escape, клику на overlay или кнопке
- Zoom изображения
- Интеграция с ArtworkCard
- Адаптивный дизайн

---

## 🔗 Интеграция компонентов

### Layout (`default.pug`)

```pug
body
  include ../components/_topbar    // Верхняя панель
  include ../components/_navbar    // Навигация
  block content
  include ../components/_modal     // Модальные окна
  include ../components/_footer    // Подвал
```

### Подключение стилей (`main.scss`)

```scss
@import 'components/topbar';
@import 'components/navbar';
@import 'components/footer';
@import 'components/artwork-card';
@import 'components/modal';
@import 'components/hero';
```

### Подключение JavaScript (`main.js`)

```javascript
import Topbar from './modules/topbar';
import Navbar from './modules/navbar';
import ArtworkCard from './modules/artwork-card';
import Modal from './modules/modal';
import Footer from './modules/footer';
```

---

## 🎯 Функциональность

### Artwork Card + Modal интеграция

1. Клик на карточку → открывает модальное окно
2. Кнопка "Просмотр" → открывает модальное окно
3. Кнопка "Лайк" → добавляет в избранное
4. Кнопка "Поделиться" → использует Web Share API или копирует ссылку

### Hover эффекты

- **Artwork Card**: Поднятие карточки, увеличение изображения, появление overlay
- **Topbar кнопки**: Изменение цвета, тени
- **Navbar ссылки**: Подчеркивание, изменение фона

### Адаптивность

Все компоненты адаптивны:
- **Desktop**: Полная функциональность
- **Tablet**: Оптимизированная сетка, меньше отступы
- **Mobile**: Вертикальное меню, полноэкранные модальные окна

---

## 📱 Пример использования на главной странице

```pug
section.gallery
  .container
    h2.section__title Популярные произведения
    .gallery__grid
      include ../components/_artwork-card
      - var artworkId = "1"
      - var title = "Звёздная ночь"
      // ... остальные переменные
```

---

## 🎨 Переменные проекта

Компоненты используют переменные из:
- `_colors.scss` - цвета проекта
- `_typography.scss` - шрифты и типографика
- `_variables.scss` - отступы, breakpoints, transitions

---

## ✨ Дополнительные возможности

- **Модальные окна** открываются автоматически при клике на карточки
- **Hover эффекты** добавляют интерактивность
- **Адаптивный дизайн** работает на всех устройствах
- **Доступность** (ARIA атрибуты, клавиатурная навигация)

---

**Все компоненты готовы к использованию! 🚀**

